
<details markdown="1">
<summary><strong>v1.2.0</strong> — Replication 커맨드 추가</summary>

**신규 커맨드 — Replication**

| 커맨드 | 설명 |
|--------|------|
| `replication list` | Replication 목록/정보 조회 |
| `replication regist` | Replication 등록 (V1/V2 분기) |
| `replication delete` | Replication 삭제 |
| `replication update` | Replication 수정 |
| `replication monit` | Replication 모니터링 |
| `replication history` | Replication 실행 히스토리 조회 |

**변경 사항**
- `license assign` — HTTP 메서드 POST → PUT 변경, 응답 출력 형식 수정
- API 응답 타입 불일치 수정 (recovery, replication)

</details>

<details markdown="1">
<summary><strong>v1.2.1</strong> — 버그 수정</summary>

- `backup regist` — `--repository-path` 선택적 변경 (미입력 + config 기본값 없어도 에러 없이 진행)
- 에러 발생 시 `--output` 옵션에 맞는 에러 메시지 출력 (기존: 출력 없음)

</details>
